#include <stdio.h>
/**
 * main - styntax start
 * Return: return value
 */
int main(void)
{
        char ch;

        for (ch = 'a' ; ch <= 'z' ; ch++)
        {
                putchar(ch);
        }
        putchar('\n');
        return (0);
}
